"""S2 — Monthly universe per spec section 3 and D3 floors.
Output: data/processed/universe.parquet, one row per (ticker, month_end).
Stops if the price table contains no delistings.

Not yet built.
"""


def main() -> None:
    raise NotImplementedError("S2 — see BACKLOG.md")


if __name__ == "__main__":
    main()
