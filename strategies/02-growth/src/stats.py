"""S4/S5 — Statistics per spec section 7 (build window) and section 8 (holdout).
Base rates, single-signal lift with Wilson 95% CI, per-regime, 2- and 3-signal
combinations, controls. D8 minimum 100 events per cell, else "insufficient".

Not yet built.
"""


def main() -> None:
    raise NotImplementedError("S4 — see BACKLOG.md")


if __name__ == "__main__":
    main()
