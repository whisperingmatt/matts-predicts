"""S2 — Ingest Sharadar tables (SF1 ARQ, SEP, DAILY, TICKERS, SF2, SF3, SP500)
incrementally into data/raw/*.parquet. Spec sections 3, 10.

Not yet built. S1 scope ends at the auth test (src/sharadar_check.py).
"""


def main() -> None:
    raise NotImplementedError("S2 — see BACKLOG.md")


if __name__ == "__main__":
    main()
