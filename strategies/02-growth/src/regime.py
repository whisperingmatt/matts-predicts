"""S3 — Regime tags per spec section 6.
SPX drawdown from prior all-time high (SEP, SPY closeadj), VIX close (CBOE CSV).
Buckets: drawdown 0–10, 10–20, 20–30, >30 percent; months-since-trough 0–12, 13–24, >24.

Not yet built.
"""


def main() -> None:
    raise NotImplementedError("S3 — see BACKLOG.md")


if __name__ == "__main__":
    main()
